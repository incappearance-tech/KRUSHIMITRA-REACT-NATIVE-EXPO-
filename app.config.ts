// Placeholder for app.config.ts - can be used for environment-specific config
export const config = {
  apiBaseUrl: process.env.EXPO_PUBLIC_API_URL || 'https://api.krushimitra.com',
  appVersion: '1.0.0',
   "plugins": [
    "@react-native-community/datetimepicker"
  ]
};
